[project]
name = "blogging-platform-api"
version = "0.1.0"
description = "A simple RESTful API for a personal blogging platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
